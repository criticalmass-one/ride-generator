nelmio_api_doc:
    documentation:
        info:
            title: Critical Mass Ride Generator
            description: Create rides for criticalmass.one
            version: 1.0.0
    areas: # to filter documented areas
        path_patterns:
            - ^/api(?!/doc$) # Accepts routes under /api except /api/doc
